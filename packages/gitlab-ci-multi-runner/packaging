# abort script on any command that exits with a non zero value
set -e

mkdir -p ${BOSH_INSTALL_TARGET}/bin

chmod +x gitlab-ci-multi-runner/gitlab-ci-multi-runner-linux-amd64-*
cp gitlab-ci-multi-runner/gitlab-ci-multi-runner-linux-amd64-* ${BOSH_INSTALL_TARGET}/bin/gitlab-ci-multi-runner

